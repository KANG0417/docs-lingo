import type { ReactElement } from "react";

export const BrandLogo = (): ReactElement => {
  return (
    <span className="text-[1.6875rem] font-extrabold tracking-tight text-white">
      DOCS-LINGO
    </span>
  );
};
